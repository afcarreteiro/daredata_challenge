[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mle_package"
version = "1.0"
authors = [
  { name="Fred", email="Fred@example.com" },
]
description = "Machine Learning Engineering Package"
readme = "README.md"
license = { file="LICENSE" }
requires-python = ">=3.9,<3.10"
dependencies = [
  "numpy<2",
  "pandas==1.4.4",
]
